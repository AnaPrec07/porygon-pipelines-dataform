config {
  type: "table",
  schema: "s_thelook",
  description: "Cleaned version of raw events table",
  tags: ["staging"]
}

select
  id as event_id,
  user_id,
  sequence_number,
  session_id,
  created_at, 
  traffic_source,
  uri,
  event_type,
from
  ${ref("events")}